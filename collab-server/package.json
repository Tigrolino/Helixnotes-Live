{
  "name": "helixnotes-collab-server",
  "private": true,
  "version": "0.1.0",
  "description": "Stages 2-4: authenticated WebSocket relay for HelixNotes real-time collaboration (transport, Yjs doc sync, and awareness/presence all ride the same opaque broadcast path). The GitHub bridge and persistence are added in a later stage.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "test": "npm run build && node --test tests/*.test.mjs"
  },
  "dependencies": {
    "dotenv": "^18.0.3",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/ws": "^8.5.12",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
