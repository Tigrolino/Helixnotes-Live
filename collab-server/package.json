{
  "name": "helixnotes-collab-server",
  "private": true,
  "version": "0.1.0",
  "description": "Stages 2-6: authenticated WebSocket relay for HelixNotes real-time collaboration (transport, Yjs doc sync, awareness/presence, file/image uploads, and live-document persistence, all riding the same opaque broadcast path, plus two plain HTTP routes for uploads).",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "test": "npm run build && node --test tests/*.test.mjs"
  },
  "dependencies": {
    "dotenv": "^18.0.3",
    "lib0": "^0.2.99",
    "ws": "^8.18.0",
    "yjs": "^13.6.27"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/ws": "^8.5.12",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
